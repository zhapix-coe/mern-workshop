
// Get list of numbers
const getValues = ()=>[1,2,3,4,5]

// Addition of two numbers
const addValues = (a,b)=> (a+b);

// Subtractoion of two numbers
const subtractValues = (a,b)=> (a-b);

export  {
    getValues, addValues,subtractValues
};
